apiVersion: v1
kind: Service
metadata:
    name: shipment-be-service
    # namespace: shipment
spec:
    type: LoadBalancer # eventualmente usa loadBalancer per prendere l'IP metto il loadbalancer e quindi lo chiamo da usalo solo keycloak
    ports:
    - port: 9091
    selector:
        app: shipment-be
